# keyboard order files

the current keyboard is the RGB revision and still needs routing.
the previous passive-keyboard fabrication and assembly files are retired;
they are available in git history.

use the current [keyboard project](../../keyboard/README.md) for the
schematic, placement and routing notes. a new order package needs the
finished routing, native DRC, copper-exclusion checks and an assembly
review covering the LEDs and switches.
